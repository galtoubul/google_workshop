import './App.css';
import { Box } from '@mui/material';
import { Kanban } from '../../components/common/kanban/kanban';

export const App = () => {
  return (
    <Box>
      <Kanban></Kanban>
    </Box>
  );
};
